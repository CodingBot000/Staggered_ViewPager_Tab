package com.exam.sample.service

import com.exam.sample.App

object ServiceActions {
    private const val prefix = "com.exam.sample."
    const val START_APP = prefix + "startApp"
    const val START_FOREGROUND = prefix + "startforeground"
    const val STOP_FOREGROUND = prefix + "stopforeground"
}

